#pragma once

#include <string>
#include <vector>

namespace Day28RegExPatternsIntroDatabasesNS
{
    std::string ltrim(const std::string &);
    std::string rtrim(const std::string &);
    std::vector<std::string> split(const std::string &);
    int day28RegExPatternsIntroDatabases();
};
