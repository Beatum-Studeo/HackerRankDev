#pragma once
#include <string>
#include <vector>

namespace Day29BitwiseANDNS
{
    std::string ltrim(const std::string &);
    std::string rtrim(const std::string &);
    std::vector<std::string> split(const std::string &);
    int bitwiseAnd(int N, int K);
    int day29BitwiseAND();
};
